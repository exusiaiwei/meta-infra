# yaml-language-server: $schema=https://aka.ms/configuration-dsc-schema/0.2
# ================================================================================================
# meta-infra - Core: Base
# ================================================================================================
# 用途：核心基础工具，所有设备必装
# 优先级：🔴 最高
#
# 安装方式：
#   winget configure manifests/core/base.yaml --accept-configuration-agreements
#
# 包含工具（6个）：
#   - Git: 版本控制
#   - Mise: 环境与任务管理
#   - Nushell: 现代化 Shell
#   - Starship: Shell 提示符美化
#   - Windows Terminal: 终端
#   - NanaZip: 压缩工具
# ================================================================================================

$schema: https://aka.ms/configuration-dsc-schema/0.2
properties:
  resources:
    # ========================================
    # Git - 版本控制
    # ========================================
    - resource: Microsoft.WinGet.DSC/WinGetPackage
      id: git
      directives:
        description: Install Git (Version Control System)
        allowPrerelease: false
      settings:
        id: Git.Git
        source: winget

    # ========================================
    # Mise - 环境与任务管理
    # ========================================
    - resource: Microsoft.WinGet.DSC/WinGetPackage
      id: mise
      directives:
        description: Install Mise (Environment + Task Manager)
        allowPrerelease: false
      settings:
        id: jdx.mise
        source: winget

    # ========================================
    # Nushell - 现代化 Shell
    # ========================================
    - resource: Microsoft.WinGet.DSC/WinGetPackage
      id: nushell
      directives:
        description: Install Nushell (Structured Shell)
        allowPrerelease: false
      settings:
        id: Nushell.Nushell
        source: winget

    # ========================================
    # Starship - Shell 提示符
    # ========================================
    - resource: Microsoft.WinGet.DSC/WinGetPackage
      id: starship
      directives:
        description: Install Starship (Cross-platform Prompt)
        allowPrerelease: false
      settings:
        id: Starship.Starship
        source: winget

    # ========================================
    # Windows Terminal
    # ========================================
    - resource: Microsoft.WinGet.DSC/WinGetPackage
      id: windows_terminal
      directives:
        description: Install Windows Terminal
        allowPrerelease: false
      settings:
        id: Microsoft.WindowsTerminal
        source: winget

    # ========================================
    # NanaZip - 压缩工具
    # ========================================
    - resource: Microsoft.WinGet.DSC/WinGetPackage
      id: nanazip
      directives:
        description: Install NanaZip (Modern Archive Tool)
        allowPrerelease: false
      settings:
        id: M2Team.NanaZip
        source: winget
  configurationVersion: 0.2.0
