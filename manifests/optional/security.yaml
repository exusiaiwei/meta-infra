# yaml-language-server: $schema=https://aka.ms/configuration-dsc-schema/0.2
# ================================================================================================
# meta-infra - Optional: Security
# ================================================================================================
# 用途：安全软件（替代 Windows Defender）
# 优先级：🔵 可选
#
# 安装方式：
#   winget configure manifests/optional/security.yaml
#
# ⚠️ 重要警告：
#   - 火绒安全软件会禁用 Windows Defender
#   - 安装前请确认您希望使用火绒替代 Windows Defender
#   - 如果已安装其他安全软件，请先卸载
#   - 不建议同时运行多个安全软件
# ================================================================================================

properties:
  configurationVersion: 0.2.0

resources:
  # ========================================
  # 火绒安全软件
  # ========================================
  - resource: Microsoft.WinGet.DSC/WinGetPackage
    id: huorong
    directives:
      description: 安装火绒安全软件
      allowPrerelease: false
    settings:
      id: Huorong.Huorong
      source: winget
    # 注意：Winget ID 需要确认
    # 火绒可能没有官方 Winget 包
    # 可能需要从官网下载
